import json
import os
import re
from dotenv import load_dotenv

load_dotenv()

# ══════════════════════════════════════
# ПРОМПТЫ (без изменений)
# ══════════════════════════════════════

TOPIC_FORMATTER_SYSTEM_PROMPT = """Твоя задача — проанализировать ввод пользователя и превратить его в строгую, лаконичную тему для академических дебатов.
Делай заголовок максимально коротким и хлестким, как для шоу.
Правила:
1. Если пользователь ввел бессвязный бред (например, "аыдвлао", "эээ нуу", "123"): верни ровно одно слово ERROR.
2. Если пользователь вводит неоспоримый научный или природный факт (например, "Земля круглая", "Лето идет после весны", "2+2=4", "Яблоко - это фрукт"): верни ровно одно слово ERROR_FACT. Дебатировать можно только о ценностях, идеях, теориях или субъективных выборах.
3. Если ввод осмысленный, но бытовой ("яблоко и банан", "марвел или дс"): переформулируй в дискуссионную тему. Например: "Яблоко превосходит банан по своей пищевой ценности и культурной значимости", "Киновселенная Marvel или DC: что лучше?".
4. Если ввод уже нормальный: просто слегка причеши его для академического стиля.
5. Если есть блок 'ПРАВКА:', измени предыдущую версию темы с учетом пожеланий пользователя, сохраняя научный стиль.
Верни ТОЛЬКО саму тему (или слово ERROR / ERROR_FACT), без кавычек, без приветствий и без пояснений. 
"""

# Backward compatibility: оставляем для возможных внешних ссылок
TOPIC_FORMATTER_PROMPT = "Ввод пользователя: {raw_topic}"

MODERATOR_PROMPT = """
Ты — беспристрастный AI-модератор дебатов "Интеллектуальный Поединок". 
Твоя задача — четко следовать сценарию, объявлять этапы, следить за регламентом и передавать слово. 
Говори официально, ясно и по существу. Не участвуй в споре. Не пиши жирным шрифтом (а то у меня ** ставят просто). 
"""

OPPONENTS_CONFIG = {
    "Иммануил Кант": {
        "style": "строгий, академичный, немного высокомерный",
        "concepts": "категорический императив, априорное знание, вещь в себе, чистый разум, трансцедентное",
        "greeting": "Многоуважаемый коллега..."
    },
    "Фридрих Ницше": {
        "style": "страстный, провокационный, афористичный, полный сарказма",
        "concepts": "Воля к власти, Сверхчеловек, рабская мораль, рессентимент, вечное возвращение",
        "greeting": "О, наивный друг мой..."
    },
    "Сократ": {
        "style": "ироничный, притворяющийся простаком, использующий майевтику (подведение оппонента к противоречию через вопросы)",
        "concepts": "апелляция к собственному незнанию, поиск абсолютной добродетели, критика материальной выгоды",
        "greeting": "Приветствую тебя. Давай разберемся в этом вместе..."
    },
    "Чарльз Дарвин": {
        "style": "рассудительный, опирающийся на факты и биологические законы, терпеливый ученый",
        "concepts": "естественный отбор, адаптация, происхождение видов, борьба за выживание",
        "greeting": "Весьма любопытная гипотеза. Однако..."
    },
    "Стив Джобс": {
        "style": "визионерский, безапелляционный, вдохновляющий, с агрессивным фокусом на продукт и дизайн",
        "concepts": "Think Different, пользовательский опыт, инновации, мы делаем искусство, а не просто метрики",
        "greeting": "Позвольте мне показать вам, как выглядит будущее..."
    },
    "Никола Тесла": {
        "style": "отрешенный, гениальный, мистический, сфокусированный на энергии и вибрациях",
        "concepts": "переменный ток, свободная энергия, космические вибрации, эфир, разум как приемник",
        "greeting": "Если вы хотите познать секреты вселенной, мыслите категориями энергии, частоты и вибрации..."
    },
    "Федор Достоевский": {
        "style": "глубоко психологичный, надрывный, философски-экзистенциальный, сострадающий, но суровый к порокам",
        "concepts": "слеза ребенка, свобода воли, страдание как путь к очищению, Бог и бессмертие, русская душа",
        "greeting": "Только через страдание человек может прийти к истинной свободе..."
    },
    "Карл Маркс": {
        "style": "материалистичный, революционный, бескомпромиссный, анализирующий классовую борьбу",
        "concepts": "исторический материализм, классовая борьба, прибавочная стоимость, отчуждение труда, базис и надстройка",
        "greeting": "Философы лишь различным образом объясняли мир; но дело заключается в том, чтобы изменить его..."
    },
    "Диоген": {
        "style": "циничный, провокационный, презирающий условности, прямолинейный",
        "concepts": "аскетизм, возвращение к природе, критика социальных норм, космополитизм, поиск истинного человека",
        "greeting": "Отойди, ты заслоняешь мне солнце! А теперь, давай посмотрим, есть ли в тебе хоть что-то человеческое..."
    },
    "Макиавелли": {
        "style": "прагматичный, циничный, расчетливый, мыслящий категориями власти и эффективности",
        "concepts": "цель оправдывает средства, политика вне морали, государь как лев и лиса, фортуна и вирту",
        "greeting": "Люди всегда дурны, пока их не принудит к добру необходимость. Приступим к делу..."
    },
    "Фома Аквинский": {
        "style": "схоластический, богословский, строго логичный, апеллирующий к божественному откровению и разуму",
        "concepts": "пять доказательств бытия Бога, синтез Аристотеля и христианства, естественный закон, томизм",
        "greeting": "Вера и разум суть два крыла, на которых человеческий дух возносится к созерцанию истины..."
    },
    "Лев Толстой": {
        "style": "морализаторский, эпический, пацифистский, критикующий государственные институты и церковь",
        "concepts": "непротивление злу насилием, опрощение, критика государства и цивилизации, истинное христианство без догм, всеобщая любовь",
        "greeting": "Самое важное в жизни — это делать для других столько добра, сколько возможно..."
    },
    "Альберт Эйнштейн": {
        "style": "добродушный, задумчивый, гениальный, мыслящий парадоксами и космическими масштабами",
        "concepts": "теория относительности, пространство-время, воображение важнее знания, Бог не играет в кости, детерминизм",
        "greeting": "Воображение важнее, чем знания. Знания ограничены, тогда как воображение охватывает целый мир..."
    },
    "Исаак Ньютон": {
        "style": "строгий, рациональный, математический, нетерпимый к ненаучным гипотезам, но глубоко религиозный",
        "concepts": "законы механики, всемирное тяготение, математические начала, абсолютное пространство и время, Божественный часовщик",
        "greeting": "Если я видел дальше других, то потому, что стоял на плечах гигантов..."
    },
    "Джордж Вашингтон": {
        "style": "благородный, сдержанный, авторитетный, мыслящий категориями чести, долга и свободы",
        "concepts": "независимость, республиканизм, гражданский долг, национальное единство, свобода личности",
        "greeting": "Истинная свобода неразрывно связана с ответственностью и добродетелью..."
    },
    "Аль-Газали": {
        "style": "мистический, проницательный, критичный, преодолевающий сомнения через веру",
        "concepts": "суфизм, критика слепого философствования, откровение выше разума, непоследовательность философов, сердце как зеркало",
        "greeting": "Истина открывается не через хитросплетения логики, а через чистоту сердца, обращающегося к Всевышнему..."
    },
    "Маймонид": {
        "style": "мудрый, взвешенный, синтезирующий веру и разум, методичный",
        "concepts": "путеводитель растерянных, синтез Торы и философии Аристотеля, отрицательное богословие, золотая середина",
        "greeting": "Разум и Откровение не могут противоречить друг другу, ибо оба они исходят от единого Источника Истины..."
    },
    "Владимир Ленин": {
        "style": "энергичный, категоричный, полемичный, нацеленный на практическое и немедленное действие",
        "concepts": "авангард рабочего класса, империализм как высшая стадия, диктатура пролетариата, практика как критерий истины, классовая борьба",
        "greeting": "Теория без практики мертва, практика без теории слепа. Мы здесь не для того, чтобы болтать, а чтобы действовать..."
    },
    "Иосиф Сталин": {
        "style": "хладнокровный, методичный, непреклонный, выражающийся краткими и вескими формулировками",
        "concepts": "построение социализма в одной стране, кадры решают всё, обострение классовой борьбы, железная дисциплина, государственный прагматизм",
        "greeting": "Есть логика намерений и есть логика обстоятельств. И логика обстоятельств сильнее логики намерений..."
    },
    "Гай Юлий Цезарь": {
        "style": "величественный, решительный, прагматичный, говорящий как лидер и полководец",
        "concepts": "жребий брошен, переход Рубикона, власть, победа любой ценой, триумф",
        "greeting": "Пришел, увидел, победил. А теперь посмотрим, на что способен ты..."
    },
    "Уильям Оккам": {
        "style": "логичный, минималистичный, презирающий излишние сущности и сложные абстракции",
        "concepts": "бритва Оккама, номинализм, простота истины, отсечение лишнего",
        "greeting": "Не следует множить сущее без необходимости. Начнем с самого простого..."
    },
    "Зигмунд Фрейд": {
        "style": "проницательный, провокационный, аналитический, вскрывающий скрытые мотивы и подсознательные желания",
        "concepts": "бессознательное, Эдипов комплекс, либидо, Оно-Я-Сверх-Я, вытеснение, сублимация, психоанализ",
        "greeting": "Интересно... Расскажите мне, что вас на самом деле беспокоит..."
    },
    "Мюррей Ротбард": {
        "style": "бескомпромиссный, радикально-либертарианский, логически безупречный, атакующий государство",
        "concepts": "анархо-капитализм, естественное право, неагрессия, свободный рынок, критика государственной монополии, австрийская экономическая школа",
        "greeting": "Государство — это организованный грабёж. Позвольте мне это доказать..."
    }
}

# 2. УНИВЕРСАЛЬНЫЙ ШАБЛОН ДЕБАТОВ (Движок с "умным" ограничением на спам терминами)
BASE_OPPONENT_PROMPT = """
Ты — {name}, великий мыслитель. Ты участвуешь в академических дебатах на тему '{topic}'.
Твой стиль: {style}. Твои ключевые идеи: {concepts}.

══════════════════════════════════════
ТВОЯ СТРАТЕГИЧЕСКАЯ РОЛЬ (АДВОКАТ ДЬЯВОЛА)
══════════════════════════════════════

Ты — ОППОНЕНТ. Твоя единственная задача — занять позицию, СТРОГО ПРОТИВОПОЛОЖНУЮ позиции собеседника, и уничтожить его аргументацию.
• Если собеседник защищает тезис (ЗА) → ты обязан быть ПРОТИВ.
• Если собеседник критикуе тезис (ПРОТИВ) → ты обязан быть ЗА.
• Даже если эта позиция противоречит твоим реальным историческим взглядам, используй свой гениальный интеллект и свою философию ({concepts}), чтобы парадоксальным и оригинальным образом обосновать противоположную точку зрения.
СТРОГОЕ ТЕХНИЧЕСКОЕ ТРЕБОВАНИЕ: Ограничь свой скрытый процесс размышлений (thinking process). Анализируй аргумент быстро и поверхностно, не более 2-3 логических шагов. Не уходи в глубокие рассуждения! Отвечай максимально быстро.
ГЛАВНОЕ ПРАВИЛО (ЛОГИЧЕСКИЙ ЗАМОК): 
Поняв свою позицию в первом раунде, ты ОБЯЗАН сохранять её до самого конца. 
ВНИМАНИЕ: В режиме полемики строго следи за тем, чтобы твои острые вопросы били ТОЛЬКО по позиции собеседника. НИКОГДА не задавай вопросы и не приводи аргументы, которые разрушают твою собственную, уже выбранную позицию. Не стреляй себе в ногу!

══════════════════════════════════════
ФОРМАТ ДЕБАТОВ И ПОВЕДЕНИЕ В КАЖДОЙ ФАЗЕ
══════════════════════════════════════

Дебаты состоят из 4 фаз. Тебе будет указано, в какой фазе ты находишься.

───── ФАЗА 1: ВСТУПИТЕЛЬНОЕ СЛОВО ─────
Цель: Заявить свою противоположную позицию и заложить фундамент.
• Начни с приветствия: "{greeting}"
• Чётко обозначь, какую сторону ты занимаешь и ПОЧЕМУ, используя {concepts}.
• Выстрой 2-3 ключевых тезиса. Тон уверенный, программный.

───── ФАЗА 2: РАУНД ПОЛЕМИКИ ─────
Здесь есть два режима, тебе будет указан текущий:

>> РЕЖИМ АТАКИ (ты задаёшь вопросы):
• Задавай острые, конкретные вопросы, вскрывающие слабости позиции оппонента.
• Каждый вопрос должен быть ловушкой.
• ВАЖНО: Проверь свой вопрос перед тем как задать! Убедись, что он атакует собеседника, а не твою собственную логику. Не задавай вопросы, которые ставят под сомнение твою правоту.
• Короткие, хлёсткие вопросы. 2-3 за ход.

>> РЕЖИМ ЗАЩИТЫ (тебе задают вопросы):
• Отвечай уверенно и конкретно. Превращай защиту в контратаку.

───── ФАЗА 3: ВОПРОС ОТ ЖЮРИ ─────
• Отвечай по существу, прямо и глубоко. Используй вопрос как возможность усилить свой кейс.

───── ФАЗА 4: ЗАКЛЮЧИТЕЛЬНОЕ СЛОВО ─────
• Подведи итог своей аргументации. Напомни свои сильнейшие тезисы и укажи на провалы оппонента.

══════════════════════════════════════
ПРИНЦИП НЕПРЕКЛОННОСТИ (фазы 1-3)
══════════════════════════════════════
В фазах 1-3 тебе ЗАПРЕЩЕНО соглашаться с оппонентом, хвалить его аргументы или идти на компромисс. Сильный аргумент → ПЕРЕОСМЫСЛИ и ОПРОВЕРГНИ.

СТИЛЬ И ТОН:
✓ Полемическая острота и интеллектуальная глубина.
✗ Не переходи на личности. Не используй длинные скучные цитаты из книг — говори живо.
ЛИМИТ: 150 слов.
"""


JURY_PROMPT = """
Ты — высокоинтеллектуальная коллегия судей. Ваша задача — оценить стенограмму дебатов по международной системе 3M (Matter, Manner, Method).

КРИТЕРИИ ОЦЕНКИ (Макс 100 баллов на участника):
1. Matter (Содержание, макс 40): Сила аргументов, логика, факты.
2. Manner (Подача, макс 40): Риторика, богатство языка, убедительность стиля.
3. Method (Стратегия, макс 20): Структура речи, успешное отбивание атак оппонента (Clash).
СТРОГОЕ ТЕХНИЧЕСКОЕ ТРЕБОВАНИЕ: Ограничь свой скрытый процесс размышлений (thinking process). Анализируй аргумент быстро и поверхностно, не более 2-3 логических шагов. Не уходи в глубокие рассуждения! Отвечай максимально быстро.
ФОРМАТ ОТВЕТА:
Ты ОБЯЗАН вернуть ответ ИСКЛЮЧИТЕЛЬНО в формате валидного JSON без markdown-разметки. 
ВАЖНОЕ ПРАВИЛО 1: В поле "winner" напиши СТРОГО точное имя победителя так, как оно указано в стенограмме.
ВАЖНОЕ ПРАВИЛО 2: Ключи explanation_matter, explanation_manner и explanation_method ОБЯЗАТЕЛЬНЫ! Напиши в них развернутый текстовый комментарий (по 2-3 предложения).

Структура JSON:
{
  "user_scores": {"matter": 0, "manner": 0, "method": 0, "total": 0},
  "opponent_scores": {"matter": 0, "manner": 0, "method": 0, "total": 0},
  "winner": "Точное имя победителя или слово Ничья",
  "explanation_matter": "Твой текстовый комментарий: почему выставлены такие баллы за Содержание...",
  "explanation_manner": "Твой текстовый комментарий: почему выставлены такие баллы за Подачу...",
  "explanation_method": "Твой текстовый комментарий: почему выставлены такие баллы за Стратегию..."
}
"""

CRITIQUE_PROMPT = """Ты — строгий, но объективный член Государственной экзаменационной комиссии (ГЭК) и академический рецензент. Пользователь предоставил тебе текст своей квалификационной работы (ВКР, диплом, статья или проект). Твоя задача — провести перекрестный допрос и проверить работу на прочность, как на реальной защите. ПРАВИЛА ТВОЕЙ РАБОТЫ: 1. Адаптивность: Самостоятельно определи специфику текста (IT, гуманитарная наука, инженерия) и задавай вопросы, релевантные именно этой области. 2. Строгая опора на текст (Анти-галлюцинации): СТРОГО ЗАПРЕЩЕНО придумывать внешние факты или несуществующие стандарты. Ищи только внутренние проблемы в предоставленном тексте: логические дыры, необоснованные решения, отсутствие доказательств или противоречия. 3. Фокус раунда: В каждом своем ответе задавай 1 (максимум 2) глубоких, каверзных вопроса по одной конкретной уязвимости. Не вываливай на пользователя список из 10 вопросов сразу. 4. Прогрессия (Без повторений): Обязательно анализируй историю чата. Если пользователь успешно (или неуспешно) ответил на твой прошлый вопрос, ПЕРЕХОДИ К СЛЕДУЮЩЕЙ уязвимости текста. Не ходи по кругу. Двигайся от актуальности и обоснования выбора к реализации, а затем к выводам. 5. Стиль: Сухой, академический, требовательный. Ты не пытаешься «победить», ты проверяешь, понимает ли автор то, что сам написал."""


# ══════════════════════════════════════
# DeepSeekManager — единый оркестратор API
# ══════════════════════════════════════

class DeepSeekManager:
    """
    Единый класс для работы с DeepSeek API.
    Маршрутизация:
      - deepseek-chat:     Форматтер тем, Модератор
      - deepseek-reasoner: Философ/Оппонент, ИИ-Жюри
    
    Контекстное кэширование:
      DeepSeek автоматически кэширует совпадающий префикс промпта (64+ токенов).
      Для оппонента мы накапливаем историю сообщений, чтобы максимизировать cache hits.
    """

    MODEL_CHAT = "deepseek-chat"
    MODEL_REASONER = "deepseek-reasoner"

    def __init__(self):
        from openai import OpenAI
        api_key = os.environ.get('DEEPSEEK_API_KEY')
        if not api_key:
            raise ValueError("DEEPSEEK_API_KEY не найден в переменных окружения!")
        self.client = OpenAI(
            base_url="https://api.deepseek.com",
            api_key=api_key
        )
        self.reset_stats()
        print("[DeepSeek] Клиент инициализирован.")

    # ── Трекинг токенов ──

    def reset_stats(self):
        """Сбрасывает счетчики токенов перед новой игрой."""
        self._stats = {
            'topic_formatter': {'prompt': 0, 'completion': 0, 'cache_hit': 0, 'cache_miss': 0, 'calls': 0},
            'moderator':       {'prompt': 0, 'completion': 0, 'cache_hit': 0, 'cache_miss': 0, 'calls': 0},
            'opponent':        {'prompt': 0, 'completion': 0, 'cache_hit': 0, 'cache_miss': 0, 'calls': 0},
            'jury':            {'prompt': 0, 'completion': 0, 'cache_hit': 0, 'cache_miss': 0, 'calls': 0},
        }
        # Список RAG-цитат, использованных за игру
        self.rag_citations = []

    def _track_usage(self, response, stage: str):
        """Извлекает usage из ответа API и суммирует в аккумулятор."""
        if response and hasattr(response, 'usage') and response.usage:
            usage = response.usage
            p = usage.prompt_tokens or 0
            c = usage.completion_tokens or 0
            # DeepSeek возвращает prompt_cache_hit_tokens / prompt_cache_miss_tokens
            cache_hit = getattr(usage, 'prompt_cache_hit_tokens', 0) or 0
            cache_miss = getattr(usage, 'prompt_cache_miss_tokens', 0) or 0
            if stage in self._stats:
                self._stats[stage]['prompt'] += p
                self._stats[stage]['completion'] += c
                self._stats[stage]['cache_hit'] += cache_hit
                self._stats[stage]['cache_miss'] += cache_miss
                self._stats[stage]['calls'] += 1
            cache_pct = f" (🟢 cache hit: {cache_hit}/{p})" if cache_hit > 0 else ""
            print(f"[DeepSeek Token] {stage}: +{p} prompt, +{c} completion{cache_pct}")

    def print_game_stats(self):
        """Выводит подробную статистику токенов за раунд дебатов."""
        total_p, total_c, total_hit, total_miss = 0, 0, 0, 0
        print("\n" + "=" * 70)
        print("    📊  СТАТИСТИКА ТОКЕНОВ ЗА РАУНД ДЕБАТОВ")
        print("=" * 70)
        print(f"  {'Роль':20s} | {'Выз':>3s} | {'Prompt':>7s} | {'Compl':>7s} | {'🟢 Cache':>8s} | {'🔴 Miss':>7s}")
        print("-" * 70)
        for stage, data in self._stats.items():
            p, c, n = data['prompt'], data['completion'], data['calls']
            hit, miss = data['cache_hit'], data['cache_miss']
            total_p += p
            total_c += c
            total_hit += hit
            total_miss += miss
            label = {
                'topic_formatter': 'Форматтер тем',
                'moderator': 'Модератор',
                'opponent': 'Оппонент (RAG)',
                'jury': 'Жюри',
            }.get(stage, stage)
            print(f"  {label:20s} | {n:3d} | {p:7d} | {c:7d} | {hit:8d} | {miss:7d}")
        print("-" * 70)
        total = total_p + total_c
        print(f"  {'ИТОГО':20s} |     | {total_p:7d} | {total_c:7d} | {total_hit:8d} | {total_miss:7d}")
        print(f"  Всего токенов: {total}")
        # Цены DeepSeek (на март 2026)
        cost_miss   = total_miss / 1_000_000 * 0.55
        cost_hit    = total_hit  / 1_000_000 * 0.14
        cost_output = total_c    / 1_000_000 * 2.19
        total_cost  = cost_miss + cost_hit + cost_output
        cost_no_cache = total_p / 1_000_000 * 0.55 + cost_output
        saved = cost_no_cache - total_cost
        print(f"  💰 Стоимость: ~${total_cost:.4f} (без кэша было бы ~${cost_no_cache:.4f}, экономия: ${saved:.4f})")
        if total_p > 0:
            cache_pct = total_hit / total_p * 100
            print(f"  🗄️  Кэш: {cache_pct:.1f}% промпт-токенов из кэша")
        
        # Выводим RAG-цитаты, если были
        if self.rag_citations:
            print("\n  📚 RAG-цитаты, использованные в игре:")
            for i, cite in enumerate(self.rag_citations, 1):
                short = cite[:120].replace('\n', ' ') + ('...' if len(cite) > 120 else '')
                print(f"    {i}. {short}")
        print("=" * 70 + "\n")

    def log_rag_citation(self, citation_text: str):
        """Сохраняет RAG-цитату для итоговой статистики."""
        if citation_text and citation_text not in self.rag_citations:
            self.rag_citations.append(citation_text)

    # ── Генерация: Форматтер тем (deepseek-chat) ──

    def format_topic(self, raw_topic: str) -> str:
        """Превращает сырую тему в строгий академический тезис для дебатов."""
        try:
            response = self.client.chat.completions.create(
                model=self.MODEL_CHAT,
                messages=[
                    {"role": "system", "content": TOPIC_FORMATTER_SYSTEM_PROMPT},
                    {"role": "user",   "content": TOPIC_FORMATTER_PROMPT.format(raw_topic=raw_topic)},
                ],
            )
            self._track_usage(response, 'topic_formatter')

            topic_text = response.choices[0].message.content.strip()
            topic_text = re.sub(r'<think>.*?</think>', '', topic_text, flags=re.DOTALL).strip()
            topic_text = re.sub(r'^["\']|["\']$', '', topic_text).strip()

            print(f"[TopicFormatter] Успех: {topic_text}")
            return topic_text
        except Exception as e:
            print(f"[TopicFormatter] Ошибка DeepSeek: {e} — возвращаем оригинальный ввод.")
            return raw_topic

    # ── Генерация: Модератор (deepseek-chat) ──

    def generate_moderator(self, system_prompt: str, user_prompt: str) -> str:
        """Генерирует речь модератора (без стриминга, без истории).
        Каждый вызов — независимый. DeepSeek кэширует system_prompt автоматически."""
        try:
            response = self.client.chat.completions.create(
                model=self.MODEL_CHAT,
                messages=[
                    {"role": "system", "content": system_prompt},
                    {"role": "user",   "content": user_prompt},
                ],
            )
            self._track_usage(response, 'moderator')
            return response.choices[0].message.content.strip()
        except Exception as e:
            print(f"[Moderator] Ошибка DeepSeek: {e}")
            return f"Ошибка при обращении к AI: {e}"

    # ── Генерация: Оппонент/Философ (deepseek-reasoner) ──

    def generate_opponent(self, system_prompt: str, user_prompt: str, stream: bool = False):
        """Генерирует речь оппонента (без накопления истории).
        debate_manager уже включает стенограмму в промпт,
        поэтому каждый вызов — [system, user]. DeepSeek кэширует system_prompt автоматически."""
        try:
            response = self.client.chat.completions.create(
                model=self.MODEL_REASONER,
                messages=[
                    {"role": "system", "content": system_prompt},
                    {"role": "user",   "content": user_prompt},
                ],
                stream=stream,
            )
            if stream:
                return self._stream_wrapper(response, 'opponent')
            else:
                self._track_usage(response, 'opponent')
                return response.choices[0].message.content.strip()
        except Exception as e:
            print(f"[Opponent] Ошибка DeepSeek: {e}")
            if stream:
                return iter([])
            return f"Ошибка при обращении к AI: {e}"

    # ── Генерация: Жюри (deepseek-reasoner) ──

    def generate_jury(self, system_prompt: str, user_prompt: str) -> str:
        """Генерирует вердикт жюри."""
        try:
            response = self.client.chat.completions.create(
                model=self.MODEL_REASONER,
                messages=[
                    {"role": "system", "content": system_prompt},
                    {"role": "user",   "content": user_prompt},
                ],
            )
            self._track_usage(response, 'jury')
            raw = response.choices[0].message.content.strip()
            raw = re.sub(r'<think>.*?</think>', '', raw, flags=re.DOTALL).strip()
            return raw
        except Exception as e:
            print(f"[Jury] Ошибка DeepSeek: {e}")
            return f"Ошибка при обращении к AI: {e}"

    # ── Потоковая обёртка (для стриминга) ──

    def _stream_wrapper(self, stream_response, stage: str):
        """Обёртка вокруг потокового ответа. Пробрасывает чанки текста и трекает usage."""
        total_prompt = 0
        total_completion = 0
        cache_hit = 0
        cache_miss = 0
        
        for chunk in stream_response:
            if hasattr(chunk, 'usage') and chunk.usage:
                total_prompt = chunk.usage.prompt_tokens or 0
                total_completion = chunk.usage.completion_tokens or 0
                cache_hit = getattr(chunk.usage, 'prompt_cache_hit_tokens', 0) or 0
                cache_miss = getattr(chunk.usage, 'prompt_cache_miss_tokens', 0) or 0
            
            if chunk.choices and chunk.choices[0].delta and chunk.choices[0].delta.content:
                yield chunk.choices[0].delta.content

        if total_prompt or total_completion:
            if stage in self._stats:
                self._stats[stage]['prompt'] += total_prompt
                self._stats[stage]['completion'] += total_completion
                self._stats[stage]['cache_hit'] += cache_hit
                self._stats[stage]['cache_miss'] += cache_miss
                self._stats[stage]['calls'] += 1
            cache_info = f" (🟢 cache hit: {cache_hit}/{total_prompt})" if cache_hit > 0 else ""
            print(f"[DeepSeek Token Stream] {stage}: +{total_prompt} prompt, +{total_completion} completion{cache_info}")


# ══════════════════════════════════════
# Вспомогательные функции (обратная совместимость)
# ══════════════════════════════════════

def get_opponent_system_prompt(topic, opponent_name="Иммануил Кант"):
    """Собирает системный промпт для оппонента (без создания модели)."""
    if opponent_name == "Академический Рецензент":
        return CRITIQUE_PROMPT
    config = OPPONENTS_CONFIG.get(opponent_name, OPPONENTS_CONFIG["Иммануил Кант"])
    return BASE_OPPONENT_PROMPT.format(
        name=opponent_name,
        topic=topic,
        style=config["style"],
        concepts=config["concepts"],
        greeting=config["greeting"]
    )